 {% for post in posts %}
                   <table>
				   <tr>
				   <td>Name</td>
				   <td>Surname</td>
				   <td>Email</td>
				   <td>Telepnone</td>
				   <td>Comment</td>
				   <td></td>
				   </tr>
				   <tr>
				   <td>{{post.first_name}}<td>
				   <td>{{post.last_name}}<td>
				   <td>{{post.email}}<td>
				   <td>{{post.phone_number}}<td>
				   <td>{{post.comment}}<td>
				   <td><a href="{{ path('edit', {'id': post.id}) }}">Edit</a></td>
				   <tr>
				   </table>
                {% endfor %}
